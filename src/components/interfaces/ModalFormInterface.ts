export interface modalFormType {
  name: string
  year: string
  companyContent: string
  companyLearnings: string
  schoolContent: string
  isDone: boolean
  date: {
    start: string
    end: string
  }
}
